<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
  <%= form.text_field :email, class: "form-control form-control-lg", placeholder: "Your Email" %>
  </div>

  <div class="form-group">
     <%= form.text_field :name, class: "form-control form-control-lg", placeholder: " Your Name" %>
  </div>

  <div class="form-group">
      <%= form.text_field :phone, class: "form-control form-control-lg", placeholder: "Your Phone Number" %>
  </div>

  <div class="form-group">
    
    <%= form.text_area :comment, class: "form-control form-control-lg", placeholder: "Your Message" %>
  </div>

  <div class="actions">
    <%= form.submit "Send Message", class: "btn btn-success" %>
  </div>
<% end %>
