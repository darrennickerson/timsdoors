<h2 class="main_title">Customer Log In</h2>
<h5>When you receive your invoice by email your log in information will be at the top.</h5>
<p>The Tim's Overhead Doors customer area is a great way to view, print and pay your invoices quickly and easily all in one place.</p>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
<div class="col-sm-12 col-md-6">
  <div class="form-group">
    
    <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'Email', autocomplete: "Email" %>
  </div>

  <div class="form-group">

    <%= f.password_field :password, autocomplete: "current-password", class: 'form-control', placeholder: 'Password' %>
  </div>
  <div class="form-group">
<div class="custom-control custom-checkbox">
<label class="custom-control-label" for="customCheck1">Remember?</label>
 <%= f.label :remember_me, class: 'custom-control-input'%>
</div>
 </div>

  <div class="actions">
    <%= f.submit "Log in", class: 'btn btn-success' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>

</div>